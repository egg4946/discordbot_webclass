# WebClass課題の解答案作成と承認後の提出

通知Bot（`npm run bot` / `npm run check`）とは別の `npm run task` コマンドです。通知Botの動作・データ（`data/state.json` など）には影響しません。
既存の `.env` の `WEBCLASS_LOGIN_URL`・`WEBCLASS_USER_ID`・`WEBCLASS_PASSWORD` でログインします。Discordの設定は不要です。

## 画面で操作する（おすすめ）

PowerShellでコマンドを打つ代わりに、ローカルのWebページから同じ流れを操作できます。

```powershell
npm run task:ui
```

`http://127.0.0.1:5173/?t=...` が既定のブラウザで開きます（`task-ui.bat` をダブルクリックしても同じです）。
ポートを変えるときは `npm run task:ui -- --port=5174`、ブラウザを開きたくないときは `-- --no-open` を付けます。

| 画面 | できること |
| --- | --- |
| 左の一覧 | 課題を締切順に表示。未取得の課題を選ぶと `fetch`（試験のときは警告と確認つき）。`一覧を更新` でWebClassから取り直します |
| 設問タブ | 問題文と、実際の入力欄（選択肢・記述欄）で解答を直接編集。ClaudeとCodexの解答・確信度・根拠が各設問の下に並び、`この解答を採用` で切り替えられます |
| 資料タブ | 取り出した資料のテキストと、PDFのページ画像（図・グラフ・表）をその場で確認 |
| レポートタブ | `report-qN.md` を編集して `保存してPDFを作り直す`。できたPDFをその場で表示 |
| 提出タブ | 未保存の編集・食い違い・確信度lowの一覧、`入力だけ試す（dry-run）`、承認、提出。WebClassの画面と提出結果も表示 |

画面の下部に操作ログが出ます（`fetch` や `answer` の進み具合はここで確認します）。

守っている約束はコマンドのときと同じです。

- WebClassやAIを動かす処理は、すべて `npm run task -- ...` を子プロセスとして実行しています。UIから別の操作や別の引数を渡すことはできません。
- 解答を1文字でも変えると承認は取り消され、承認済みハッシュと一致する解答だけが `提出する` で送れます。
- 承認ボタンを押すと、提出する解答の全文が確認画面に出ます。提出は赤いボタンと確認画面の2段階です。
- サーバーは `127.0.0.1` だけで待ち受け、起動時のトークンが付いたURLからしか使えません（同じPCの他のブラウザタブや外部サイトからは操作できません）。トークン付きURLは他人に渡さないでください。
- 提出中（`submit`）だけは中止ボタンを出しません。採点ボタンの押下がWebClassへ届いている可能性があるためです。

## 流れ（コマンド）

```powershell
npm run task -- fetch "ソフトウェア工学基礎 第1回 復習課題(2)"   # 設問と資料を取得（task-id が表示される）
npm run task -- answer <task-id> both                             # Claude と Codex で解答案を作成
npm run task -- render <task-id>                                  # 任意: レポート本文を直したらPDFを作り直す
npm run task -- review <task-id>                                  # 解答案を確認（review.md にも保存）
npm run task -- dry-run <task-id>                                 # 任意: WebClassに入力して画面を保存（提出しない）
npm run task -- approve <task-id> <承認用ハッシュ>                 # 本人が確認した解答版だけを承認
npm run task -- submit <task-id>                                  # 採点ボタンを押して提出
```

UIを使わない場合は次のコマンドです。課題名は「授業名と課題名に含まれる言葉」をスペース区切りで指定します（全角・半角は区別しません）。複数一致したときは候補とIDが表示されるので、IDで指定し直します。`npm run task -- list` で課題の一覧も出せます。

## 各コマンド

| コマンド | 内容 |
| --- | --- |
| `fetch` | 課題を開いて［開始］を押し、設問を読み取ります。同じ授業の「講義資料」から課題名の「第N回」に合う章のPDFと、「第N回」を含む資料を取得し、テキストとページ画像の両方を保存します。`--material <資料名>` で資料を追加、`--no-materials` で資料なし。提出処理に入った課題（`submitting` / `submitted` / `unverified`）は `--force` を付けない限り取り直しません（付けた場合も前回の提出記録は `previousSubmission` に残ります）。 |
| `answer` | `claude` / `codex` / `both`（標準）。両方の場合は並列に解かせ、一致しない設問に ⚠ を付けます。食い違った設問は `--prefer`（標準 `claude`）の解答を仮に入れます。 |
| `select` | `select <task-id> codex 3 7` のように、指定した設問だけ（省略時は全設問）を片方のAIの解答に差し替えます。レポートの設問なら、そのAIの本文でPDFも作り直します。 |
| `render` | `report-qN.md` を編集したあとにPDF（`report-qN.pdf`）を作り直し、`answer.json` のパスを更新します。提出ファイルが変わるため、承認は取り消されます。 |
| `review` | 全設問・提出する解答・各AIの解答と根拠・承認用ハッシュを表示します。 |
| `dry-run` | WebClassの問題画面に解答を入力し、入力内容を読み戻して確認してから `dry-run.png` を保存します。採点は押しません。WebClassは採点を押すまで入力を保存しないため、提出にはなりません。 |
| `approve` | `review` に表示されたハッシュを指定します。`answer.json` を1文字でも変えるとハッシュが変わり、再承認が必要です。 |
| `submit` | 承認済みの解答だけを入力し、読み戻し確認・`before-submit.png` の保存後に［採点］を押します。取得時と設問が変わっていたら中止します。結果画面を確認できなかった場合は `unverified` になり、自動では再試行しません。 |

解答は `data/tasks/<task-id>/answer.json` を直接編集しても構いません（Git管理外）。形式は次の通りです。

| 設問の形式（`questions.md` の [形式]） | `values` |
| --- | --- |
| `text`（記述欄） | 記述欄ごとの文字列 `["Quality","Cost","Delivery"]` |
| `textarea`（記述式） | 記述欄ごとの文章。文字数の下限・上限がある場合は検査されます |
| `checkbox`（複数選択） | 選ぶ選択肢の番号 `["5","9","10"]`、選ばないなら `[]` |
| `radio`（単一選択） | 番号1つ `["3"]` |
| `select`（プルダウン・組み合わせ） | (1)(2)...の順の番号 `["3","4","2"]` |
| `file`（ファイル提出） | 課題フォルダ内のファイルの相対パス `["report.pdf"]` |

### ファイル提出（レポート）の設問

ファイル提出の設問は、AIが**提出するレポート本文**を書き、それをPDFにして提出します。

1. `answer` で、各AIが本文をMarkdownで作り、`report-<AI名>-q<設問番号>.md` に保存します。
2. 採用したほう（`--prefer`、標準は `claude`）の本文が `report-q<設問番号>.md` になり、Chromiumで `report-q<設問番号>.pdf` に変換され、`answer.json` の `values` にそのファイル名が入ります。
3. `review` には、提出するレポート本文と、もう一方のAIの本文が表示されます。**必ず読んで確認してください。**
   経験・意見・感想を書かせる設問も空欄にはせず、講義資料に即した解答案を作らせます。履修時期のような本人の事実は「日本の高校の標準的な課程」などの一般的な場合を仮定して書かせ、仮定した内容はAIの根拠欄に「要確認:」として並びます。自分に当てはまるかを確かめて直してください。
4. 直したいときは `report-q<設問番号>.md` を編集して `npm run task -- render <task-id>` を実行します（PDFを作り直し、承認は取り消されます）。自分で用意したPDFを使う場合は、ファイルを課題フォルダに置いて `answer.json` の `values` を書き換えても構いません。

PDFの先頭には「授業名 課題名」が入ります。氏名・学籍番号を入れたい場合は `.env` に `TASK_REPORT_AUTHOR=学籍番号 氏名` を設定してください（AIには渡りません）。使えるMarkdownは見出し・箇条書き・表・強調・コードだけで、AIには数式もLaTeXではなく通常の文字で書くよう指示しています。

アップロード欄が複数ある設問だけは、AIは本文を作りません。ファイルを `data/tasks/<task-id>/` に置き、`answer.json` の `values` に相対パスを並べてください。

承認用ハッシュには、`answer.json` の内容だけでなく**提出するファイルの中身**も含まれます。承認後にPDFを作り直すと `submit` は止まり、再承認が必要になります。
`dry-run` ではファイルが存在するかだけを確認し、アップロードはしません（WebClassはファイルを選んだ時点でアップロードするため）。`submit` では、ファイルを先にアップロードして提出済みになったことを確かめてから、残りの設問を入力して［採点］を押します。

## AI（ClaudeとCodex）について

APIキーは使いません。このPCにログイン済みの **Claude Code CLI（`claude`）** と **Codex CLI（`codex`）** を、それぞれのサブスクリプションで呼び出します。
CLIを起動するときは `ANTHROPIC_API_KEY`・`OPENAI_API_KEY` などを環境から取り除くため、従量課金のAPIに切り替わることはありません。WebClassのパスワードやDiscordのトークンもAIには渡しません。

- Codex: `npm install -g @openai/codex` → `codex` を起動してChatGPTアカウントでログイン
- Claude: `npm install -g @anthropic-ai/claude-code` → `claude` を起動してClaudeアカウントでログイン

AIは課題フォルダを読み取り専用で参照し、`questions.md`・`materials/*.txt`・`materials/images/`（PDFの各ページ画像）を読んで解答します。

**図・グラフ・表は、PDFから取り出したテキストには含まれません。** そのため `fetch` の時点で、資料PDFの全ページを画像（`materials/images/<資料名>/pNNN.png`）にして保存し、AIには「図が関係する設問では画像を開いて数値を読むこと」と指示しています。画像化はPlaywrightのChromiumで行うため、追加のソフトは不要です。
使用するモデルは CLI の個人設定に関係なく固定しています。

| AI | モデル | 推論の強さ |
| --- | --- | --- |
| Claude | `claude-opus-5-5` | `medium` |
| Codex | `gpt-6-sol` | `medium` |

変更したい場合だけ `.env` に `TASK_CLAUDE_MODEL` / `TASK_CLAUDE_EFFORT` / `TASK_CODEX_MODEL` / `TASK_CODEX_EFFORT` を設定します。`review` には、各解答を作ったモデルが表示されます。CLIが標準以外の場所にある場合は `CLAUDE_CLI_PATH` / `CODEX_CLI_PATH` で指定できます。

AIの解答が一致していても正解とは限りません。特に、図やグラフの読み取りが必要な設問、授業独自の用語を問う設問は資料で確認してください。

## 対応範囲と安全策

- 対応: 1ページに全設問が表示される自習・テスト・レポート形式（記述欄・記述式・複数選択・単一選択・プルダウン・組み合わせ・ファイル提出）。設問の形式は、WebClassが表示した入力欄の種類から判定します。
- 対応: 「次のページ」「終了」で1ページずつ進むテスト（例: 電子工学基礎「第1回の小テスト」）。コマンドは1ページ形式と同じです。設問番号はページ順に 1, 2, ... と振り直します（WebClassの「Q.1」「Q.2」と同じ）。
- 未対応: 入力欄の種類が判定できない設問。未対応として表示され、提出には進みません。
- `fetch` が対応していない画面で止まったときは、各フレームのHTMLを `data/tasks/<task-id>/pages/` に保存します（対応を追加するときの資料になります）。

### 1ページずつ進むテストでの注意

WebClassは、別のページへ移動した時点でそのページの解答を保存します（採点は［終了］を押したときだけです）。そのため各コマンドは次のように動きます。

- `fetch`: 全ページを順に開いて設問を読み取ります。何も入力しないので、保存されるのは開く前と同じ内容です。
- `dry-run`: ページごとに入力して読み戻し、`dry-run-p1.png`, `dry-run-p2.png`... を保存したあと、元の入力に戻してから次のページへ進みます。元に戻せなかった場合はページを移動せずに中止します（そのページの入力は保存されません）。
- `submit`: ページごとに、ファイル提出の設問があればまず［レポートを提出］でアップロードし（この時点でWebClassに保存されます）、そのあと残りの入力・読み戻し・`before-submit-pN.png` の保存をして次へ進みます。全ページを入力したら、もう一度全ページを開き、WebClassに保存された解答が承認した内容と一致することを確かめてから［終了］を押します。途中で止まった場合、採点はされていませんが入力済みのページは保存されています（`submit` をやり直すと上書きされます）。
- 記述式とファイル提出は、実際のWebClassの課題ではまだ動作確認できていません（画面のしくみから作成）。初めて使う課題では `dry-run` の画面を確認し、提出後は `submission-receipt.txt` を確認してください。
- 提出処理に入った課題（`submitting` / `submitted` / `unverified`）では、`answer` / `select` / `render` / `approve` はすべて止まります。解答を作り直して再び提出できる状態に戻すのは `fetch --force` だけです。
- ファイル提出は、欄の表示がアップロード前から変わったことを確かめます。前回の提出で残っている「提出済」だけでは成功とみなさず、採点の前に中止します。
- 両方のAIがレポート本文を書いた設問は、本文が必ず異なるため常に ⚠（本文を読み比べる）として扱います。
- 「試験」「テスト」「小テスト」や、開始画面に制限時間・回数制限の記載がある教材は、受験回数を消費する可能性があるため `--allow-attempt` を付けない限り開きません（`dry-run` / `submit` も同様）。
- 授業内課題など、学内IPアドレス限定・時間限定の教材は自宅からは開けません。
- 課題データ（設問・資料・解答・スクリーンショット）は `data/tasks/` に保存され、Gitには含まれません。

## Discordから使う場合（今後）

各処理は `src/task-fetch.js`・`src/task-answer.js`・`src/task-submit.js` の関数として呼び出せるようにしてあります。
Discordから指示する場合も、`fetch → answer → review（Discordに解答を表示）→ 所有者がボタンで承認 → submit` の順にし、承認・提出は所有者だけに限定する想定です。
AIのCLIはサブスクのログインが必要なため、VPSではなくこのPCで処理を実行する構成を想定しています。
